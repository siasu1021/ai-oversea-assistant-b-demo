# AI 出海助手 · 版本 B Demo

静态 H5 演示页面，入口为 `index.html`。部署到 GitHub Pages、Vercel、Netlify 或任意静态托管后即可通过手机浏览器直接访问。

## Files

- `index.html` 页面入口
- `styles.css` 版本 B 视觉规范与动效
- `app.js` 演示交互状态机
- `IP光球_眨眼.gif` 首页光球 IP
- `光球_加载状态_loading-memo.png` 初始化加载/失败光球

## Deploy Notes

GitHub Pages 可直接托管仓库根目录。`.nojekyll` 已添加，避免静态资源被 Jekyll 处理。
